// ... (previous imports and setup remain the same)

const contractAddress = "YOUR_PROXY_CONTRACT_ADDRESS"; // Replace with the address of the TransparentUpgradeableProxy

// ... (rest of the server.js code remains the same)